# LLM GUI 文件维护清单

用途：快速了解项目结构与文件职责。方法级细节与检索锚点见 `llm_gui.py` 顶部维护目录。

## 仓库内文件（入库发布）

| 文件 | 功能 |
|------|------|
| `llm_gui.py` | 全部功能主程序（单文件，customtkinter） |
| `gemma_chat_template.jinja` | Gemma 聊天模板，`--chat-template-file` 使用 |
| `presets_template.json` | 预设模板（JSONC 带注释），可复制成 llm_presets.json 或从「设置→导入预设」合并。**入库，但默认不进发布资产**（需要者自行下载） |
| `app.ico` | 打包图标 |
| `README.md` | 中文说明 |
| `docs/en/README.md` | 英文说明 |
| `screenshot.jpg` | README 界面预览图 |

## 本地文件（gitignore 不入库）

> ⚠️ 前三个 JSON（`llm_presets.json` / `llm_default_presets.json` / `llm_gui_config.json`）是**软件运行时自动生成**的个性化文件，**不发布**；只按文件名绑定，位置跟随 `BASE` 目录（exe 版 = exe 所在目录）自动生成/更新。

| 文件 | 功能 |
|------|------|
| `models/` | GGUF 模型目录，程序启动时扫描 |
| `llama-server.exe` + `*.dll` | llama.cpp 运行时，须与程序同目录（BASE=exe 目录） |
| `llm_presets.json` | **软件运行时生成，不发布**：预设数据 `{模型: {预设名: 参数}}`（支持 JSONC 注释，可直接手写） |
| `llm_default_presets.json` | **软件运行时生成，不发布**：自动计算参数缓存（显存+模型大小算出的默认值） |
| `llm_gui_config.json` | **软件运行时生成，不发布**：本机配置：窗口/sash、硬件缓存、categories、server_path（llama 路径全局项）、gemma（自动判断覆盖）、last_model/last_preset、overwrite_ask/remember_size |
| `llm_gui_err.log` | `main()` 崩溃栈写入 |
| `LLMGUI.spec` | 本机 PyInstaller 打包配置（`*.spec` 已 ignore） |
| `build/` `dist/` `__pycache__/` | 构建/运行产物 |

## llm_gui.py 内部模块（检索锚点）

| 模块 | 功能 | 检索锚点 |
|------|------|----------|
| 全局常量 | 路径 / CTX_PRESETS / PARALLEL_OPTS / PARAMS / 模板 / VERSION | `MODELS_DIR =` `PARAMS =` `VERSION =` |
| 文件读写 | 三个 JSON 的 load/save + JSONC 去注释 | `def load_cfg` `def load_presets` `def _strip_jsonc` `def _load_jsonc` |
| 硬件检测 | detect_gpus / detect_vram_gb | `def detect_gpus` |
| ToolTip | 悬浮提示 | `class ToolTip` |
| App.UI | 全部控件构建 + 参数区网格 | `def _build_ui` |
| App.模型数据 | 扫描/切换模型、gemma 自动判断、多模态投影匹配、自动计算 | `def on_model_change` `def is_gemma` `def find_mmproj` `def compute_defaults` |
| App.预设 | 存 / 删 / 改名 / 导入导出 / 孤儿 / 批量 | `def save_preset` `def import_presets` `def manage_orphan_presets` `def manage_presets_all` |
| App.参数读写 | UI ↔ 参数 dict（含 thinking/gemma；空=不传） | `def set_params` `def read_params` |
| App.命令组装 | llama-server 命令行 | `def build_cmd` |
| App.启停 | start / stop / 日志读取 | `def start` |
| App.后台线程 | 队列分发 | `def _poll_q` |
| App.弹窗 | 设置 / 覆盖确认等 | `def open_settings` | 设置页「📦 模型清单」按钮 → `def show_model_list`（弹出独立窗口列 models/ 所有模型+大小+总占用） |
| 入口 | main（校验 exe 后启动） | `def main` |

## 关键约定

- **参数区行号**：右列从 row0 起（与左列顶部对齐：Flash=row0 与上下文长度同行、温度=row1、最大输出=row2）；左列 row2 起。思考模式 `(row3, col2)`、推理预算 `(row4, col2)`、多模态+投影文件 `(row5, col2)`、Gemma `(row6, col2)` 硬编码；简单参数左列行由 `len(PARAMS)` 自动推（base=6）。增删 PARAMS 项后须手动检查这四个硬编码位置。llama 路径不在参数区（在「设置」）。
- **参数归属**：`thinking` / `gemma` / `mm` 是**预设参数**（存进 llm_presets.json 的每条预设）。thinking 预设省略时按「开」处理；mm 预设省略时按「关」；gemma 预设省略时回退 `is_gemma()`（按文件名 + cfg 的 gemma 覆盖字典自动判断）。旧版按模型存 cfg 的 thinking 已废弃。
- **多模态**：勾选「多模态」→ 启动带 `--mmproj`。投影文件 = 多模态同一行的下拉（row5,col2）：`（自动）` 走 `find_mmproj` 按文件名 token 匹配（单个直接用、多个选最像的、都不像返回 None 并日志提示）；选具体文件 = 手动指定，按模型存 `cfg['mmproj'][model]`（全局项、不进预设，`build_cmd` 优先用手动值）。`mmproj` 文件本身不作为可选模型列出（见 `models_list`）。
- **留空规则**：所有预设参数可留空（`''`）= 不传给 llama-server，用 llama 自身默认。下拉参数（parallel/host/flash/cache）用「（默认）」项表示留空；gpu 用「自动」。端口留空时界面显示按 8080 兜底。
- **llama 路径**：全局项，存 `cfg['server_path']`，在「设置」对话框设置（可浏览选择）；`build_cmd` 与 `main()` 校验用 `cfg.get('server_path') or SERVER_EXE`。
- **窗口尺寸**：用 `self.geometry()`（逻辑尺寸），勿用 `winfo_width/height`（物理尺寸 × DPI 缩放会复合放大）。
- **加 llama-server 参数**：命令行只在 `build_cmd` 加；若是可调参数须同步 `set_params` / `read_params` / `compute_defaults`（以及预设模板 presets_template.json）。
- **发布资产**：无特殊改动默认 Release 仅含 `LLMGUI.exe` 单文件；`presets_template.json` 等辅助文件在仓库内（入库），有需要的用户自行下载，不进发布包。
- **本地打包**：`pyinstaller LLMGUI.spec` 打包后覆盖运行时目录下的 `LLMGUI.exe`（须与 llama-server.exe + models/ 同目录）。**覆盖前不备份旧 exe**——回退旧版本直接 git 拉取 GitHub 重打包。
- **llama-server 思考参数**：当前 b10303 用 `--reasoning on|off|auto`（旧版 `--no-thinking` 已弃用）。`on` 已实测：非推理模型无害，Murasaki 等推理模型会输出 `reasoning_content`。
